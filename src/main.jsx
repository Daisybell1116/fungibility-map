import React from "react";
import ReactDOM from "react-dom/client";
import LiveFungibilityMap from "./LiveFungibilityMap.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
<React.StrictMode>
<LiveFungibilityMap />
</React.StrictMode>
);
